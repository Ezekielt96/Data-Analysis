#Quiz 
##How to write function python 
##How to access data through slicing 
##how to calculate min, max 
##standard deviation 
##functions , slcing 
##Lambda, Autonomous functions 
##
##*************************************
##Finding Max 
max value = 0 #set a low bar so you replace any items with new max value
for item in dataObject:
	if item > maxValue :
	nextValue ==item 


MinValue = 1000000
rowCounter = 0
MinIndex = 0 
for item in columnList[1]
	if item < minvalue
	minIndex = rowCounter 
rowCounter +=1 

print ("Hotel" +columnList[3].get(minIndex)

##********************************************************
#standard deviation 
1. calculate the average 
2. Initialize the standard dviation List 

#1
sum = 0 
for item in enumerate (columnList[i]):
	sum = sum +int (item)
avg = Sum]len(columnList[1])

sdevList = [[ ], [    ] ]

for col in range(columnCount) :
    emptySet = []
    sdevList.append(emptySet)
#The Standard deviation is the value minus avg value Squared 
for line in wordSet :
            for index, value in enumerate (line) :
                if (index == 0 ) :
                    continue 
                stDev = pow (avgList[index]-value,2)
                stdGreValue = sdevList[index]
                sdevList[index].append(stDev)
#**********************************************************
#Autonomous function 
mySet = [ 12, 4, 7, 6, 8, 10, 23, 15 ]


for value in mySet :
	if value % 2 == 1 :
		print(value)

def findOddValues(currSet) :
	oddSet = [ value for value in currSet if value % modulusNumber == 0 ] 
	return oddSet


newSet = findOddValues(mySet)

modulusNumber  = 3
print(newSet)

